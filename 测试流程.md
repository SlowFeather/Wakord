D:/APP/Anaconda3/Scripts/activate
conda activate wakeup
wakeup listen --show-score     # 对麦克风说「小元」,看实时分数,定阈值
wakeup serve --listen          # 起常驻服务并立即监听
# 另开一个终端用程序/命令控制:
wakeup ctl start | stop | status
wakeup events                  # 持续接收「唤醒命中」事件





# 首次：准备数据（含多音色扩充），慢，跑一次
wakeup prepare --gen-voices

# 之后反复调参，只跑这个（跳过所有合成/提取）
wakeup fit --epochs 10        # 先少跑几轮看趋势
wakeup fit                    # 满意了跑满 50 轮出正式模型

# 新增了录音/音色后，重建特征缓存再训
wakeup record --count 30
wakeup prepare --force-features
wakeup fit


uv run --frozen --extra dev wakeup listen --show-score


uv run --frozen --extra dev wakeup listen --debug

uv run --frozen --extra dev wakeup listen --show-score --threshold 0.3





cd D:\Project\Python_Project\Chat\WakeUp

# 1. 看 wake 服务状态
uv run wakeup ctl status

# 2. 如果 ready=False 且提示缺 xiaoyuan.onnx，先训练/导出模型
uv run --extra train wakeup train

# 如果要用 Edge TTS 多音色扩充，再用这个，耗时更久
uv run --extra all wakeup train --gen-voices

# 3. 确认模型生成
Test-Path .\models\xiaoyuan.onnx

# 4. 前台直接测麦克风和分数
uv run wakeup listen --show-score