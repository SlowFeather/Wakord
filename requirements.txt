# Core runtime + training dependencies.
# Keep this file ASCII-only for older conda builds on zh-CN Windows.

# Common
numpy<2
scipy
soundfile
pyyaml
tqdm

# Audio capture + light VAD
sounddevice
webrtcvad-wheels

# Inference runtime + wake-word engine
onnxruntime
openwakeword

# ONNX export support for recent torch versions
onnxscript

# Training: Chinese TTS generation + classifier
sherpa-onnx
torch

# Optional Edge TTS voice expansion used by `wakeup gen-voices`
edge-tts
